/**
 * Re-export of the canonical theme hook. `useTheme()` returns the resolved
 * `Theme` object — `{ scheme, colors, shadows }` — driven by the ThemeProvider.
 */

export { useTheme, useThemeContext } from '@/theme';
